{
  "compilerOptions": {
    /* --- Configuration de base pour Node/Express --- */
    "target": "es2016",             // Version de JS générée
    "module": "commonjs",           // Le standard pour Node.js
    "moduleResolution": "node",     // INDISPENSABLE pour qu'il trouve tes dossiers node_modules

    /* --- Chemins (à ajuster si tes fichiers ne sont pas dans src) --- */
    // "rootDir": "./src",
    // "outDir": "./dist",

    /* --- Interopérabilité --- */
    "esModuleInterop": true,        // Permet les imports simples (import express from 'express')
    "allowSyntheticDefaultImports": true,

    /* --- Strictness (Gardons ça propre mais sans excès) --- */
    "strict": true,
    "skipLibCheck": true,           // Ignore les erreurs dans les fichiers de définition des autres
    "forceConsistentCasingInFileNames": true,

    /* --- IMPORTANT : On supprime la ligne "types": [] --- */
    // En ne mettant rien ici, TypeScript va charger AUTOMATIQUEMENT
    // tous les types présents dans node_modules/@types (express, cors, node...)
  },
  "include": ["src/**/*"],          // Assure-toi que cela pointe vers ton code
  "exclude": ["node_modules"]
}